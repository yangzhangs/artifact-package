### 2. 必须经过验证 | Must Be Verified

**即使代码是 AI 生成的，也必须在本地运行并验证功能正确。** 未经验证的 PR 将直接关闭。

<mark>**Even if the code is AI-generated, you must run and verify it locally.** Unverified PRs will be closed.</mark>
